# 🚀 Руководство по настройке NewsBot

## 📋 Содержание
- [Быстрый старт](#быстрый-старт)
- [Проблема с кириллицей](#проблема-с-кириллицей)
- [Ручная настройка](#ручная-настройка)
- [Использование](#использование)
- [Решение проблем](#решение-проблем)

---

## ⚡ Быстрый старт

### Windows PowerShell (рекомендуется)

```powershell
# 1. Автоматическая настройка проекта
.\setup.ps1

# 2. Отредактируйте .env и добавьте токен бота
notepad .env

# 3. Запустите бота
.\run.ps1
```

### Windows CMD

```cmd
# 1. Создайте окружение
python -m venv venv

# 2. Активируйте окружение
activate.bat

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Настройте .env файл
copy .env.example .env
notepad .env

# 5. Запустите бота
python main.py
```

---

## ⚠️ Проблема с кириллицей

### 🔴 КРИТИЧНО: Кириллица в ПУТИ к проекту

**Плохо:**
```
C:\Users\Пользователь\Desktop\проекты\newsBot\     ❌
C:\Документы\Python\newsBot\                       ❌
D:\Мои проекты\newsBot\                            ❌
```

**Хорошо:**
```
C:\Users\User\Desktop\projects\newsBot\            ✅
C:\Projects\newsBot\                               ✅
D:\Dev\newsBot\                                    ✅
```

### Почему это проблема?

1. **Python инструменты** (pip, virtualenv) плохо работают с Unicode в путях Windows
2. **Некоторые библиотеки** не могут корректно обработать путь с кириллицей
3. **Возникают ошибки:**
   - `UnicodeDecodeError`
   - `FileNotFoundError` (даже если файл существует)
   - Проблемы при установке пакетов
   - Сбои импорта модулей

### 🟢 НЕ проблема: Кириллица ВНУТРИ проекта

**Безопасно использовать:**
- ✅ В комментариях к коду
- ✅ В строковых литералах
- ✅ В docstrings
- ✅ В логах
- ✅ В сообщениях бота

```python
# ✅ Это работает нормально
logger.info("🚀 Запуск NewsBot")
message = "Привет, пользователь!"
"""Модуль для парсинга новостей"""
```

### Решение проблемы

Если ваш проект находится в папке с кириллицей:

1. **Переместите проект** в папку без кириллицы
2. **Пересоздайте виртуальное окружение**:
   ```powershell
   # Удалите старое окружение
   Remove-Item -Recurse -Force venv
   
   # Создайте новое
   python -m venv venv
   
   # Установите зависимости
   .\venv\Scripts\pip.exe install -r requirements.txt
   ```

---

## 🔧 Ручная настройка

### 1. Проверка Python

```powershell
python --version
# Требуется Python 3.8 или выше
```

### 2. Создание виртуального окружения

```powershell
# Создание окружения
python -m venv venv

# Активация в PowerShell
.\venv\Scripts\Activate.ps1

# Активация в CMD
venv\Scripts\activate.bat
```

### 3. Установка зависимостей

```powershell
# Обновление pip
python -m pip install --upgrade pip

# Установка зависимостей
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

```powershell
# Создайте .env файл
copy .env.example .env

# Отредактируйте и добавьте токен
notepad .env
```

Пример `.env`:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
DATABASE_PATH=newsbot.db
LOG_LEVEL=INFO
SCHEDULE_MODE=manual
DEFAULT_INTERVAL=hourly
```

---

## 🎯 Использование

### Готовые скрипты

**PowerShell:**
```powershell
# Настройка проекта (первый раз)
.\setup.ps1

# Активация окружения
.\activate.ps1

# Запуск бота
.\run.ps1

# Парсинг новостей
.\run.ps1 parse

# Тестирование парсера
.\run.ps1 test
```

**CMD:**
```cmd
# Активация окружения
activate.bat

# Запуск бота
python main.py

# Парсинг новостей
python parse_and_save_news.py

# Тестирование
python test_parser.py
```

### Команды бота в Telegram

После запуска бота:

- `/start` - Приветствие и список команд
- `/news` - Получить 4 последние новости
- `/news 10` - Получить 10 новостей
- `/digest` - Дайджест лучших новостей за период

---

## 🔍 Решение проблем

### Ошибка: "Execution of scripts is disabled"

```powershell
# Разрешите выполнение скриптов (запустите PowerShell от администратора)
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Ошибка: "TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here не найден"

1. Убедитесь, что файл `.env` существует
2. Проверьте, что токен указан в формате:
   ```
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
   ```
3. Получите токен у @BotFather в Telegram

### Ошибка: "Module not found"

```powershell
# Убедитесь, что окружение активировано
.\activate.ps1

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Проблемы с кодировкой в PowerShell

```powershell
# Установите кодировку UTF-8
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
```

### База данных заблокирована

```powershell
# Остановите все процессы Python
taskkill /F /IM python.exe

# Удалите файл блокировки (если есть)
Remove-Item newsbot.db-wal, newsbot.db-shm -ErrorAction SilentlyContinue
```

---

## 📦 Структура проекта

```
newsBot/
├── bot/                    # Модуль Telegram бота
│   ├── handlers.py        # Обработчики команд
│   └── scheduler.py       # Планировщик рассылки
├── config/                # Конфигурация
│   ├── settings.py        # Настройки проекта
│   └── sources.py         # RSS источники
├── database/              # База данных
│   ├── db_manager.py      # Менеджер БД
│   └── models.py          # Модели данных
├── parser/                # Парсинг новостей
│   ├── rss_parser.py      # RSS парсер
│   ├── html_parser.py     # HTML парсер
│   └── ranking.py         # Ранжирование новостей
├── utils/                 # Утилиты
│   ├── formatter.py       # Форматирование сообщений
│   └── logger.py          # Логирование
├── logs/                  # Логи
├── venv/                  # Виртуальное окружение
├── main.py               # Точка входа (запуск бота)
├── parse_and_save_news.py # Скрипт парсинга
├── test_parser.py        # Тесты парсера
├── requirements.txt      # Зависимости Python
├── .env                  # Переменные окружения (создайте!)
├── .env.example          # Пример .env
├── setup.ps1             # Автоматическая настройка
├── run.ps1               # Запуск бота (PowerShell)
├── activate.ps1          # Активация окружения (PowerShell)
└── activate.bat          # Активация окружения (CMD)
```

---

## 🎓 Дополнительные команды

### Обновление зависимостей

```powershell
pip install --upgrade -r requirements.txt
```

### Экспорт зависимостей

```powershell
pip freeze > requirements.txt
```

### Очистка базы данных

```powershell
Remove-Item newsbot.db
# БД пересоздастся при следующем запуске
```

### Просмотр логов

```powershell
Get-Content logs\newsbot.log -Tail 50
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте, что проект находится в папке **без кириллицы** в пути
2. Убедитесь, что виртуальное окружение **активировано**
3. Проверьте наличие и правильность `.env` файла
4. Посмотрите логи в папке `logs/`

---

**Удачного использования NewsBot! 🚀**

