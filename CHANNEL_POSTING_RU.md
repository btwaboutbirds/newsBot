# 📢 Автопостинг в Telegram канал - Краткая инструкция

## 🎯 Что было добавлено

Бот теперь умеет автоматически публиковать дайджесты новостей в ваш Telegram канал!

## ⚡ Быстрый старт

### 1. Создайте файл `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
CHANNEL_ID=-1003303731185
DATABASE_PATH=newsbot.db
LOG_LEVEL=INFO
SCHEDULE_MODE=manual
DEFAULT_INTERVAL=hourly
```

### 2. Добавьте бота в канал

1. Откройте ваш канал в Telegram
2. Перейдите в **Администраторы** → **Добавить администратора**
3. Найдите бота и добавьте его
4. Дайте боту право **Публиковать сообщения**

### 3. Проверьте доступ

```bash
python test_channel_access.py
```

Если все работает, вы увидите тестовое сообщение в канале!

### 4. Запустите парсинг с автопостингом

```bash
python parse_and_save_news.py
```

Бот:
- ✅ Спарсит новости из RSS
- ✅ Сохранит топ-4 в базу
- ✅ Автоматически отправит дайджест в канал

## 🎮 Способы постинга

### Вариант 1: Автоматический (при парсинге)

```bash
python parse_and_save_news.py
```

Каждый раз при запуске этого скрипта дайджест автоматически отправится в канал.

### Вариант 2: Ручной (через бота)

1. Запустите бота: `python main.py`
2. Отправьте боту: `/post_digest`
3. Дайджест будет опубликован в канале

### Вариант 3: По расписанию (автоматический режим)

В `.env` измените:
```env
SCHEDULE_MODE=auto
DEFAULT_INTERVAL=hourly
```

Запустите бота: `python main.py`

Теперь бот будет **каждый час**:
1. Парсить новости
2. Сохранять в базу
3. Постить дайджест в канал

Доступные интервалы:
- `hourly` - каждый час
- `3hours` - каждые 3 часа
- `6hours` - каждые 6 часов
- `daily` - раз в день

## 📊 Формат дайджеста

Дайджест содержит топ-4 новости:

```
⭐ Топ-4 новостей за последний час:

1. Заголовок новости
[Читать далее](ссылка)
Краткое описание...
⭐ 8.5/10
```

- Markdown разметка ✅
- Заголовки жирным шрифтом ✅
- Описания курсивом ✅
- Рейтинг каждой новости ✅

## 🔧 Настройки

Все настройки в файле `.env`:

| Параметр | Описание | Пример |
|----------|----------|--------|
| `TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here` | Токен бота от @BotFather | `123456:ABC...` |
| `CHANNEL_ID` | ID канала (начинается с `-100`) | `-1003303731185` |
| `SCHEDULE_MODE` | Режим: `manual` или `auto` | `manual` |
| `DEFAULT_INTERVAL` | Интервал парсинга | `hourly` |

## ❓ Частые вопросы

**Q: Как узнать ID канала?**  
A: Используйте бота [@userinfobot](https://t.me/userinfobot) - перешлите ему сообщение из канала.

**Q: Бот не может постить в канал**  
A: Убедитесь что:
- Бот добавлен как администратор
- У бота есть право "Публиковать сообщения"
- CHANNEL_ID правильный

**Q: Можно ли изменить количество новостей?**  
A: Да, в `config/settings.py` измените `TOP_NEWS_COUNT = 4`

**Q: Как часто постить?**  
A: Это зависит от `DEFAULT_INTERVAL` в `.env`. По умолчанию - каждый час.

## 📝 Новые файлы

- `bot/channel_poster.py` - модуль для постинга в канал
- `test_channel_access.py` - скрипт проверки доступа
- `CHANNEL_SETUP.md` - подробная документация
- `.env.example` - шаблон конфигурации

## 🎉 Готово!

Теперь ваш канал будет автоматически получать дайджесты новостей!

Подробная документация: [CHANNEL_SETUP.md](CHANNEL_SETUP.md)

