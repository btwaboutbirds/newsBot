# 📢 Настройка автоматического постинга в Telegram канал

Этот документ содержит инструкции по настройке автоматического постинга дайджестов новостей в Telegram канал.

## 📋 Требования

1. ✅ Telegram бот создан и токен получен
2. ✅ Telegram канал создан
3. ✅ Бот добавлен в канал как администратор

## 🔧 Настройка

### Шаг 1: Создайте файл `.env`

Скопируйте `.env.example` в `.env`:

```bash
copy .env.example .env
```

### Шаг 2: Заполните `.env` файл

Откройте `.env` и укажите ваши данные:

```env
# Токен вашего бота (получен от @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ID вашего канала
CHANNEL_ID=-1003303731185

# Остальные настройки можно оставить по умолчанию
DATABASE_PATH=newsbot.db
LOG_LEVEL=INFO
SCHEDULE_MODE=manual
DEFAULT_INTERVAL=hourly
```

### Шаг 3: Добавьте бота в канал

1. Откройте ваш канал в Telegram
2. Нажмите на название канала → **Administrators**
3. Нажмите **Add Administrator**
4. Найдите вашего бота и добавьте его
5. Убедитесь, что у бота есть право **Post Messages**

### Шаг 4: Проверьте доступ

Запустите тестовый скрипт:

```bash
python test_channel_access.py
```

Если все настроено правильно, вы увидите:
- ✅ Информацию о боте
- ✅ Подтверждение доступа к каналу
- ✅ Тестовое сообщение в вашем канале

## 🚀 Использование

### Автоматический постинг при парсинге

Запустите скрипт парсинга, который автоматически отправит дайджест в канал:

```bash
python parse_and_save_news.py
```

Этот скрипт:
1. 🔍 Парсит новости из RSS источников
2. 📊 Ранжирует их по релевантности
3. 💾 Сохраняет топ-4 в базу данных
4. 📢 Автоматически отправляет дайджест в канал

### Ручной постинг через бота

Если бот запущен (`python main.py`), вы можете вручную отправить дайджест в канал:

1. Отправьте боту команду `/post_digest`
2. Бот подготовит дайджест и отправит его в канал
3. Вы получите уведомление об успешной отправке

### Автоматический режим (расписание)

Для настройки автоматического постинга по расписанию:

1. Откройте `.env`
2. Измените режим:
   ```env
   SCHEDULE_MODE=auto
   DEFAULT_INTERVAL=hourly  # или '3hours', '6hours', 'daily'
   ```
3. Запустите бота: `python main.py`

Бот будет автоматически:
- 🔍 Парсить новости каждый час (или другой интервал)
- 📢 Постить дайджест в канал
- 💾 Сохранять новости в базу данных

## 📊 Формат дайджеста

Дайджест включает топ-4 новости с:
- **Заголовком** (жирным шрифтом)
- **Ссылкой** "Читать далее"
- **Кратким описанием** (курсивом)
- **Рейтингом** новости (⭐ 0.0-10.0)

Пример:

```
⭐ Топ-4 новостей за последний час:

1. Новая версия Python 3.12 выпущена
[Читать далее](https://example.com/news/1)
Python 3.12 включает множество улучшений производительности...
⭐ 8.5/10

2. GitHub представил новые функции для разработчиков
[Читать далее](https://example.com/news/2)
...
```

## 🛠️ Команды

| Команда | Описание |
|---------|----------|
| `python test_channel_access.py` | Проверить доступ к каналу |
| `python parse_and_save_news.py` | Парсинг + автопостинг |
| `python main.py` | Запустить бота |
| `/post_digest` | Вручную постить дайджест (в боте) |

## ❓ Troubleshooting

### Бот не может отправить сообщение в канал

**Ошибка:** `Chat not found` или `Forbidden`

**Решение:**
1. Убедитесь, что CHANNEL_ID правильный (начинается с `-100`)
2. Проверьте, что бот добавлен в канал как администратор
3. Убедитесь, что у бота есть право "Post Messages"

### Как узнать ID канала?

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в канал
2. Переслайте любое сообщение из канала боту
3. Бот покажет ID канала (начинается с `-100`)

### Новости не появляются

**Возможные причины:**
1. RSS источники недоступны
2. В источниках нет новых новостей
3. Проверьте логи: `logs/newsbot.log`

## 📝 Дополнительная информация

- Периодичность постинга = периодичность парсинга (настраивается в `.env`)
- Дайджест всегда содержит топ-4 новости по рейтингу
- Дубликаты новостей автоматически исключаются
- Все действия логируются в `logs/newsbot.log`

## 🔐 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте файл `.env` в Git
- Храните токен бота в секрете
- `.env` уже добавлен в `.gitignore`

---

**Готово!** Теперь ваш бот будет автоматически постить дайджесты новостей в канал. 🎉

