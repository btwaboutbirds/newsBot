# Telegram Bot Configuration
# @BotFather РІ Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_hereyour_bot_token_from_botfather
CHANNEL_ID=your_channel_id

# Database Configuration
DATABASE_PATH=newsbot.db

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Schedule Mode (manual РёР»Рё auto)
SCHEDULE_MODE=manual

# Default Schedule Interval (hourly, 3hours, 6hours, daily)
DEFAULT_INTERVAL=hourly
