# NewsBot 🤖📰

Telegram бот для автоматизированного сбора, ранжирования и рассылки новостей на тему "Vibe Coding".

## Возможности

- 📡 Автоматический сбор новостей из RSS-лент
- ⭐ Интеллектуальное ранжирование по релевантности
- 📢 **Автопостинг дайджестов в Telegram канал** 🆕
- 🕐 Гибкая настройка расписания рассылки
- 🌍 Поддержка английского и русского языков
- 💾 Хранение в SQLite с проверкой уникальности

## Технологический стек

- **Python 3.11+**
- **aiogram 3.13.1** - асинхронный Telegram Bot фреймворк
- **feedparser 6.0.11** - парсинг RSS
- **beautifulsoup4 4.12.3** - парсинг HTML
- **aiohttp 3.10.10** - асинхронные HTTP запросы
- **SQLite3** - база данных

## ⚡ Быстрая установка (Windows)

### Автоматическая настройка
```powershell
# Запустите один скрипт - и всё готово!
.\setup.ps1
```

Скрипт автоматически:
- ✅ Проверит Python
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Создаст шаблон .env файла
- ✅ Проверит структуру проекта

После этого:
1. Отредактируйте `.env` и добавьте токен бота
2. Запустите: `.\run.ps1`

---

## ⚠️ ВАЖНО: Проблема с кириллицей

**Проект ДОЛЖЕН находиться в папке без кириллицы в пути!**

❌ **Плохо:**
```
C:\Users\Пользователь\Desktop\проекты\newsBot\
D:\Мои документы\Python\newsBot\
```

✅ **Хорошо:**
```
C:\Users\User\Desktop\projects\newsBot\
C:\Projects\newsBot\
D:\Dev\newsBot\
```

**Почему:** Python инструменты (pip, virtualenv) и некоторые библиотеки плохо работают с Unicode в путях Windows.

**Кириллица в коде (комментарии, строки) - это нормально!** ✅

📖 Подробнее: [SETUP_GUIDE.md](SETUP_GUIDE.md)

---

## 📦 Установка (ручная)

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd newsBot
```

### 2. Создание виртуального окружения
```bash
python -m venv venv
```

### 3. Активация виртуального окружения

**Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
# или используйте готовый скрипт:
.\activate.ps1
```

**Windows (CMD):**
```cmd
venv\Scripts\activate.bat
# или используйте готовый скрипт:
activate.bat
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

### 4. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

Скопируйте файл-пример и отредактируйте его:
```bash
# Windows
copy .env.example .env
notepad .env

# Linux/macOS
cp .env.example .env
nano .env
```

Заполните реальными значениями:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
CHANNEL_ID=your_channel_id
DATABASE_PATH=newsbot.db
LOG_LEVEL=INFO
SCHEDULE_MODE=manual
DEFAULT_INTERVAL=hourly
```

**Как получить токен бота:**
1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

## 🚀 Запуск

### Простой способ (Windows)

```powershell
# Запуск бота
.\run.ps1

# Парсинг и сохранение новостей
.\run.ps1 parse

# Тестирование парсера
.\run.ps1 test
```

### Ручной запуск

**1. Активируйте виртуальное окружение:**

**Windows (PowerShell):**
```powershell
.\activate.ps1
# или
.\venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
activate.bat
# или
venv\Scripts\activate.bat
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

**2. Запустите бота или парсинг:**

**Способ 1: Прямой вызов Python (рекомендуется для Windows):**
```bash
# Основной бот
.\venv\Scripts\python.exe main.py

# Парсинг новостей (с автопостингом в канал)
.\venv\Scripts\python.exe parse_and_save_news.py

# Тестирование парсера
.\venv\Scripts\python.exe test_parser.py

# Проверка доступа к каналу
.\venv\Scripts\python.exe test_channel_access.py
```

**Способ 2: Через активированное окружение:**
```bash
# Основной бот
python main.py

# Парсинг новостей
python parse_and_save_news.py

# Тестирование
python test_parser.py
```

При успешном запуске вы увидите:
```
🚀 Запуск NewsBot
📊 Инициализация базы данных...
⚙️ Настройка обработчиков команд...
⏰ Инициализация планировщика...
✅ Бот запущен: @your_bot_username
👂 Ожидание сообщений...
```

## Команды бота

- `/start` - Начало работы с ботом
- `/news` - Получить свежие новости (по умолчанию 4)
- `/digest` - Получить дайджест лучших новостей за период
- `/post_digest` - Отправить дайджест в канал (для администраторов) 🆕

## 📢 Автопостинг в канал

Бот теперь умеет автоматически публиковать дайджесты новостей в ваш Telegram канал!

### Быстрая настройка:

1. **Добавьте ID канала в `.env`:**
   ```env
   CHANNEL_ID=your_channel_id
   ```

2. **Добавьте бота в канал как администратора** с правом "Публиковать сообщения"

3. **Проверьте доступ:**
   ```bash
   python test_channel_access.py
   ```

4. **Запустите парсинг с автопостингом:**
   ```bash
   # Windows (рекомендуется)
   .\venv\Scripts\python.exe parse_and_save_news.py
   
   # Или через активированное окружение
   python parse_and_save_news.py
   ```

Бот автоматически:
- ✅ Парсит новости из RSS
- ✅ Сохраняет топ-4 в базу
- ✅ Отправляет дайджест в канал

### Режимы работы:

**1. Автопостинг при парсинге (рекомендуется):**
```bash
# Windows
.\venv\Scripts\python.exe parse_and_save_news.py

# Или через активированное окружение
python parse_and_save_news.py
```

**2. Ручной постинг через бота:**
Отправьте боту команду `/post_digest`

**3. Автоматический режим по расписанию:**
Измените в `.env`:
```env
SCHEDULE_MODE=auto
DEFAULT_INTERVAL=hourly
```
Запустите: `python main.py`

📖 **Подробная документация:** [CHANNEL_POSTING_RU.md](CHANNEL_POSTING_RU.md) | [CHANNEL_SETUP.md](CHANNEL_SETUP.md)

## 📁 Структура проекта

```
newsBot/
├── bot/                       # Логика Telegram бота
│   ├── handlers.py           # Обработчики команд
│   ├── scheduler.py          # Расписание рассылки
│   └── channel_poster.py    # 🆕 Постинг в канал
├── parser/                   # Парсинг новостей
│   ├── rss_parser.py        # Парсинг RSS
│   ├── html_parser.py       # Парсинг HTML
│   └── ranking.py           # Ранжирование
├── database/                 # Работа с БД
│   ├── models.py            # Модели данных
│   └── db_manager.py        # Управление БД
├── config/                   # Конфигурация
│   ├── settings.py          # Настройки
│   └── sources.py           # RSS источники
├── utils/                    # Утилиты
│   ├── logger.py            # Логирование
│   └── formatter.py         # Форматирование
├── logs/                     # Логи работы
├── venv/                     # Виртуальное окружение
├── main.py                   # Точка входа (запуск бота)
├── parse_and_save_news.py   # Скрипт парсинга новостей с автопостингом
├── clear_database.py         # Утилита для очистки базы данных
├── test_parser.py           # Тестирование парсера
├── test_channel_access.py   # Проверка доступа к каналу
├── requirements.txt         # Python зависимости
├── setup.ps1                # Автоматическая настройка проекта
├── run.ps1                  # Удобный запуск (PowerShell)
├── activate.ps1             # Активация окружения (PowerShell)
├── activate.bat             # Активация окружения (CMD)
├── .env.example             # Пример конфигурации
├── SETUP_GUIDE.md           # Подробное руководство по настройке
├── CHANNEL_POSTING_RU.md    # Автопостинг в канал (краткая инструкция)
├── CHANNEL_SETUP.md         # Автопостинг в канал (подробная документация)
├── PARSER_USAGE.md          # Использование парсера
├── QUICKSTART.md            # Быстрый старт
└── README.md                # Документация
```

## 🛠️ Дополнительные скрипты

### Windows PowerShell

```powershell
# Первоначальная настройка проекта
.\setup.ps1

# Активация окружения
.\activate.ps1

# Запуск бота
.\run.ps1

# Парсинг новостей
.\run.ps1 parse

# Тестирование
.\run.ps1 test
```

### Windows CMD

```cmd
# Активация окружения
activate.bat

# Запуск бота
python main.py
```

## 💻 Разработка

Проект следует модульной архитектуре с разделением ответственности:
- Все операции асинхронные (async/await)
- Код следует PEP 8
- Type hints для всех функций
- Docstrings в формате Google Style

## 🛠️ Утилиты

### Очистка базы данных

Если нужно очистить базу данных от всех новостей:

```bash
# Windows
.\venv\Scripts\python.exe clear_database.py

# Или через активированное окружение
python clear_database.py

# Автоматическое подтверждение (без запроса)
python clear_database.py --yes
```

## 📚 Документация

- **[SETUP_GUIDE.md](SETUP_GUIDE.md)** - Подробное руководство по установке и настройке
- **[CHANNEL_POSTING_RU.md](CHANNEL_POSTING_RU.md)** - Автопостинг в канал (краткая инструкция)
- **[CHANNEL_SETUP.md](CHANNEL_SETUP.md)** - Автопостинг в канал (подробная документация)
- **[PARSER_USAGE.md](PARSER_USAGE.md)** - Использование парсера
- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт

## Лицензия

MIT License

## btwaboutbirds

Alex / @btwaboutbirds

